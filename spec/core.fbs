include "game_state.fbs";
include "render.fbs";

namespace rocketsim;

table Connection {}

table Quit {}

table Speed {
    speed: float;
}

table Paused {
    paused: bool;
}

union Message {
    Connection,
    Quit,
    Speed,
    Paused,
    GameState,
    AddRender,
    RemoveRender,
}

table Packet {
    message: Message (required);
}
